
#include <stdio.h>

char *decode(char *buffer, char *result) 
{
	int offset = 0;



int main(void)
{
char content[10000];
	decode(encoding, content);
	FILE * file = fopen("limits.txt", "w");
	fprintf(file, "%s", content);
	printf("%i", *"0x5604a51382e0");
}